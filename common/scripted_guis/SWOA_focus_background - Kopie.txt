scripted_gui = {
	lobby_background = {
		context_type = player_context
		parent_window_name = "player_lobby_window"
		window_name = "lobbybackground"

		visible = {
			always=yes
		}

		properties = {
			lobbybackgroundimage = {
				image = "[GLOBAL.GetSuperEventPicture]"
			}
		}
	}
